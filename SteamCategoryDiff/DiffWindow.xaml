<UserControl x:Class="SteamCategoryDiff.DiffWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="StatusCellBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding}" Value="Yes">
                    <Setter Property="Background" Value="#2F5E2F"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding}" Value="No">
                    <Setter Property="Background" Value="#A15400"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding}" Value="--">
                    <Setter Property="Background" Value="#6A6A6A"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DashCellBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding}" Value="--">
                    <Setter Property="Background" Value="#6A6A6A"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="StatusCellTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <Style x:Key="DashCellTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding}" Value="--">
                    <Setter Property="Foreground" Value="White"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,8">
            <CheckBox Content="Steam only" IsChecked="{Binding ShowSteamOnly}" Margin="0,0,10,0"/>
            <CheckBox Content="Playnite only" IsChecked="{Binding ShowPlayniteOnly}" Margin="0,0,10,0"/>
            <CheckBox Content="Both" IsChecked="{Binding ShowBoth}" Margin="0,0,10,0"/>
            <CheckBox Content="Show identicals" IsChecked="{Binding ShowIdenticals}" Margin="0,0,12,0"/>
            <Button Content="Reload" Command="{Binding LoadCommand}" Margin="0,0,8,0" />
            <Button Content="Copy report" Command="{Binding CopyReportCommand}" />
        </DockPanel>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Items}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Application name" Binding="{Binding Name}" Width="2*"/>

                <DataGridTemplateColumn Header="PN?" Width="50">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Padding="8,3" Style="{StaticResource StatusCellBorderStyle}" DataContext="{Binding AvailableInPlayniteText}">
                                <TextBlock Text="{Binding}" HorizontalAlignment="Center" Style="{StaticResource StatusCellTextStyle}"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Steam?" Width="50">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Padding="8,3" Style="{StaticResource StatusCellBorderStyle}" DataContext="{Binding AvailableInSteamText}">
                                <TextBlock Text="{Binding}" HorizontalAlignment="Center" Style="{StaticResource StatusCellTextStyle}"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Steam app ID" Binding="{Binding AppId}" Width="80"/>

                <DataGridTextColumn Header="Playnite categories" Width="2*"  Binding="{Binding PlayniteCategoriesText}" />

                <DataGridTextColumn Header="Steam categories" Width="2*"  Binding="{Binding SteamCategoriesText}" />

                <DataGridTemplateColumn Header="OK?" Width="50">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Padding="8,3" Style="{StaticResource StatusCellBorderStyle}" DataContext="{Binding CategoriesSameText}">
                                <TextBlock Text="{Binding}" HorizontalAlignment="Center" Style="{StaticResource StatusCellTextStyle}"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
